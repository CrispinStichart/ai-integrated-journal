If a failure of the environment (for example, a missing tool, permissions issues, or lack of access to a web resouce) prevents you from following a plan, do NOT continue. Stop and inform the user of the problem.

After finishing a discrete unit of work (feature, bug fix, documentation update, etc)., and after all linting and tests have passed, commit the change and push to main. You don't have to run the full test suite if your'e not editing source files; e.g. if you're only editing markdown files, you just have to format them.
