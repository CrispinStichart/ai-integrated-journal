# Task 55 coverage remediation handoff

## Objective

Add meaningful behavioral tests for product code already committed at base `9fcf793f6de11875e86755f71373433c630e066a` so every configured coverage metric exceeds its threshold, without modifying the preserved dirty task-55 implementation/docs/scripts. Validate both the dirty task-55 aggregate and an isolated base-plus-test-delta worktree, then commit only coverage tests, push `main`, and verify local/origin/remote equality.

## Base and git state

- Workspace: `/workspaces/ai-integrated-journal`
- Branch: `main`
- Base `HEAD`: `9fcf793f6de11875e86755f71373433c630e066a`
- Base `origin/main`: `9fcf793f6de11875e86755f71373433c630e066a`
- Existing task-55 work is dirty and must be preserved exactly.
- Existing untracked `.task-55-handoff.md` must never be staged during this coverage subtask.
- This file must never be staged or committed.

## Behavioral targets

- Cover real behavior, failures, privacy/security, recovery, ownership, invariants, or integration semantics in already committed product code.
- Do not add import/existence/line-execution/mock-without-outcome/compliance tests.
- Do not change coverage thresholds, exclusions, collection, product implementation, task-55 docs, or task-55 scripts.

## Files

- Coverage-test files: not yet selected.
- Preserved task-55 files: all files shown dirty/untracked at startup; capture and verify their exact state before staging/commit.

## Commands and results

- `git status --short --branch`: `main...origin/main`; existing task-55 modifications and untracked files present.
- `git rev-parse HEAD`: `9fcf793f6de11875e86755f71373433c630e066a`.
- `git rev-parse origin/main`: same as HEAD.
- No coverage changes made yet.

## Remaining steps

1. Read `AGENTS.md`, `high-level-technical-overview.md`, `docs/implementation-plan.md`, `.task-55-handoff.md`, and inspect committed tests/config/product code plus preserved dirty work.
2. Capture a robust snapshot/checksum of all pre-existing dirty task-55 paths.
3. Identify meaningful low-cost behavioral branch coverage in base product code.
4. Add tests only and run targeted tests/coverage.
5. Run full `corepack pnpm validate` in the dirty task-55 workspace.
6. Create an isolated worktree at base plus only the test delta and run full `corepack pnpm validate` there.
7. Verify pre-existing dirty task-55 state is unchanged; stage only coverage-test files; commit one test-only commit; push main.
8. Verify local HEAD, origin/main, and remote main are equal.
9. Append final coverage commit hash/tests/results to `.task-55-handoff.md`, then remove this coverage handoff without staging either handoff.

## Risks

- Coverage is close to the branch threshold (reported 74.75% vs 75%), so tests must exercise genuine uncovered branches without relying on dirty task-55-only code.
- Existing tracked test files are already modified by task 55; avoid them unless exact separation can be guaranteed. Prefer untouched test files or new test files.
- Full validation may be long; update this journal after milestones and long checks.
